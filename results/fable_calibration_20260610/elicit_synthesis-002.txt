Estimate the probability that you could complete this task correctly in one attempt.
Return JSON only with fields: p_success (0..1), estimated_tokens_total (total model tokens for one full solve attempt), rationale (optional).

Task ID: synthesis-002
Title: Adversarial debate: monorepo vs polyrepo with forced balance
Description:
A 200-person engineering organization is deciding between a monorepo (single repository for all services and libraries) and a polyrepo (one repository per service). They currently have 15 microservices, 8 shared libraries, and deploy roughly 20 times per day.

Construct the strongest possible case FOR the monorepo (4 arguments), then the strongest possible case AGAINST it, i.e. for polyrepo (4 arguments). Each side must address:
- CI/CD pipeline design
- Code sharing and dependency management
- Team autonomy
- Developer onboarding

Then provide a balanced recommendation that honestly acknowledges what the organization would be giving up with either choice.

Acceptance commands:
- LLM_JUDGE: a GPT-5.2 judge (temperature 0) scores your single answer against a hidden rubric; pass requires score >= 7/10

Strategy guidance:
Give your direct best estimate with a concise rationale.